### Quick little vibe-coded session chat for Gemini. 

Use a .env file with an API_KEY variable name for your Gemini API key. 

You can use ```!g``` to enable grounding, which will get sources in the output. 

Run ```build.sh``` to setup your virtual environment. 
